# Questions

## 1. Type inference

```rust
let guess: u64 = "42".parse().expect("Not a number!");
```

## 2. Why this happens exactly

```rust
let x = 0.1 + 0.2;
println!("precision = {}", x);
```

## 3. What 2's compliment?

## 4. Any changes in the number representation changes in Rust compared to other languages?
